# Design QA

- 标注参考：`/Users/a1/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_67kb4xju005n21_3fe5/temp/InputTemp/2ebc56b5-dab4-44b6-a684-5035f81575a6.png`
- 修改前浏览器截图：`output/design-qa/openwork-user-chrome-current.png`
- 修改后浏览器截图：`output/design-qa/openwork-user-chrome-after.png`
- 同图对照：`output/design-qa/openwork-user-chrome-comparison.png`
- 探索页美国默认视角：`output/design-qa/openwork-user-chrome-us-explore.png`
- 浏览器：用户当前 Chrome，页面真实 CSS 区域 2133 × 1031，设备像素比约 1.8。
- 密度归一：参考图 3822 × 1854 按设备像素比等比归一到 2133 × 1031；实现截图由浏览器直接输出为 2133 × 1031。
- 状态：首页入场完成；进入探索模式后未选择岗位，地球默认正对美国本土。

## 对照结论

- 标题：保持 112px 字号、两行结构和原有字距，仅将整组从 y≈369 上移到 y≈292，上移约 77px。
- 按钮：保持胶囊轮廓与橙色圆形箭头，宽度从 388px 缩到 340px，高度从 88px 缩到 76px，文字同步调整到 16px。
- 地球：首页海拔由 1.34 调到 1.14，视觉直径增大约 18%，左边缘与用户标注框接近，顶部和底部自然裁切，只展示局部。
- 品牌、背景、色板、图钉、页脚和探索页结构均保持不变。

## 必查视觉面

- 字体：标题字体、字重、行高和两行断句未改变；按钮缩小后仍清晰，不显拥挤。
- 布局：标题、按钮、地球三者均按标注方向调整，左侧文案和右侧地球之间仍保留足够留白。
- 色彩：未修改现有浅蓝背景、莫兰迪大陆和橙色按钮强调色。
- 图像质量：继续使用实时 3D 地球与真实图钉，没有放大静态截图；放大后国家边界仍清晰。
- 文案：标题和按钮文字保持不变。

## 对照过程

1. P1：修改前地球几乎完整显示，不符合“放大、只要局部”的标注。修复后地球半径明显增加，上下自然越出窗口。
2. P2：修改前标题组 y≈369，视觉重心偏低。修复后移动到 y≈292，与标注方向一致。
3. P2：修改前按钮 388 × 88px，体量偏重。修复后为 340 × 76px，仍保留清楚的点击区域。
4. 修改后同图对照未发现新的 P0、P1 或 P2 问题。

## 交互与运行

- 点击“探索工作世界”后可正常进入探索状态。
- 无选择状态默认定位到美国本土（北纬 38°、西经 98°），美国岗位点位居中且密集可见。
- 点击重置后会回到同一美国默认视角。
- 图钉默认视觉比例降至原基础比例的约 60%，聚合图钉约 66%，密集城市不再互相遮挡。
- 悬停图钉会在 0.21 秒内放大到默认状态的 1.75 倍；离开后平滑缩回。
- 可点击的隐形感应范围同步扩大，图钉变小后仍然容易悬停和点击。
- 搜索框出现；未选择岗位时不会提前显示岗位卡片。
- 首页与探索页均无横向或纵向溢出。
- 新建 Chrome 验证页未出现控制台错误。
- `npm run test:smoke`、`npm run build:xhs`、`npm run validate:xhs`、`git diff --check` 全部通过。

## 剩余差异

- P3：地球会持续缓慢旋转，因此停留较久后大陆位置会与静态标注图产生轻微变化；这是既有交互设定。

final result: passed
